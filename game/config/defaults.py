"""Default config values shared by the new architecture."""

from game.validator.scoring_config import SCORING_INTERVAL

DEFAULT_SCORING_INTERVAL = SCORING_INTERVAL
DEFAULT_GAME_INTERVAL = "1 minutes"
DEFAULT_PROTOCOL_VERSION = "brainplay.game@1"
